import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import { ReButton } from "@reservando/design-system";

<Meta
  title="Components/Button"
  component={ReButton}
  argTypes={{
    size: { control: { type: "select", options: ["small", "medium", "large"] } },
    modifier: { control: { type: "select", options: ["cta-red", "cta-uva", "outline-uva"] } },
    type: { type: "select", options: ["button", "submit"] },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { ReButton },
  template: '<ReButton :size="size" :modifier="modifier">{{label}}</ReButton>',
});

# ReButton

<Canvas>
  <Story name="basic">
    {{
      components: { ReButton },
      template: "<ReButton>My Button</ReButton>",
    }}
  </Story>
</Canvas>

<ArgsTable of={ReButton} />

## How to use

```html
<template>
  <ReButton @click="">Label</ReButton>
</template>

<script lang="ts">
  import { ReButton } from "@reservando/design-system";
</script>
```

## Using modifiers

<Canvas>
  <Story name="modifiers">
    {{
      components: { ReButton },
      data() {
        return {
          checked: true,
        };
      },
      template: `
      <div>
        <ReButton modifier="cta-red">cta-red</ReButton>
        <ReButton modifier="cta-uva">cta-uva</ReButton>
        <ReButton modifier="outline-uva">outline-uva</ReButton>
      </div>
    `,
    }}
  </Story>
</Canvas>

## Using sizes

<Canvas>
  <Story name="sizes">
    {{
      components: { ReButton },
      data() {
        return {
          checked: true,
        };
      },
      template: `
      <div>
        <ReButton size="small">small</ReButton>
        <ReButton size="medium">medium</ReButton>
        <ReButton size="large">large</ReButton>
      </div>
    `,
    }}
  </Story>
</Canvas>
