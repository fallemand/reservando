import { Meta, Story } from "@storybook/addon-docs/blocks";
import Code from "./assets/code-brackets.svg";
import ReservandoLogo from "@reservando/design-system/images/logo-ui.svg";

<Meta title="Introduction/Getting started" />

<style>{`
  .logo {
    width: 300px;
  }
  .subheading {
    --mediumdark: '#999999';
    font-weight: 900;
    font-size: 14px;
    color: #999;
    letter-spacing: 4px;
    line-height: 24px;
    text-transform: uppercase;
    margin-bottom: 12px;
    margin-top: 40px;
  }

  .link-list {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr 1fr;
    row-gap: 10px;
  }

  .link-item {
    display: block;
    padding: 20px 30px 20px 15px;
    border: 1px solid #00000010;
    border-radius: 5px;
    transition: background 150ms ease-out, border 150ms ease-out, transform 150ms ease-out;
    color: #333333;
    display: flex;
    align-items: flex-start;
  }

  .link-item:hover {
    border-color: #1EA7FD50;
    transform: translate3d(0, -3px, 0);
    box-shadow: rgba(0, 0, 0, 0.08) 0 3px 10px 0;
  }

  .link-item:active {
    border-color: #1EA7FD;
    transform: translate3d(0, 0, 0);
  }

  .link-item strong {
    font-weight: 700;
    display: block;
    margin-bottom: 2px;
  }

  .link-item img {
    height: 40px;
    width: 40px;
    margin-right: 15px;
    flex: none;
  }

  .link-item span {
    font-size: 14px;
    line-height: 20px;
  }
`}</style>

<img class="logo" src={ReservandoLogo} alt="Logo" />

The simple, intuitive and powerful ui library to develop faster great frontends at **[Reservando.club](https://reservando.club)**

<div className="subheading">Installation</div>

```sh
npm i @reservando/design-system --save
```

Set up font and global styles

```css
@import "~@reservando/design-system/styles/variables";

.body {
  color: $text;
  font-family: $font-family;
  font-size: $base-font-size;
  -webkit-font-smoothing: antialiased;
}
```

Start using components! 😃

<Story id="components-button--basic" />

<div className="subheading">Get in touch</div>

<div className="link-list">
  <a className="link-item" href="https://github.com/fallemand/reservando" target="_blank">
    <img src={Code} alt="code" />
    <span>
      <strong>GitHub project</strong>
      View the source and add issues
    </span>
  </a>
</div>
