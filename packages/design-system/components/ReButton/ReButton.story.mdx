import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import { ReButton } from "@reservando/design-system";

<Meta
  title="Components/Button"
  component={ReButton}
  argTypes={{
    size: { control: { type: "select", options: ["small", "medium", "large"] } },
    modifier: { control: { type: "select", options: ["cta-red", "cta-uva", "outline-uva"] } },
    type: { type: "select", options: ["button", "submit"] },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { ReButton },
  template: '<ReButton :size="size" :modifier="modifier">{{label}}</ReButton>',
});

# ReButton

<Canvas>
  <Story name="basic">
    {{
      components: { ReButton },
      template: "<ReButton>My Button</ReButton>",
    }}
  </Story>
</Canvas>

<ArgsTable of={ReButton} />

## How to use

```html
<template>
  <ReButton @click="clickHandler">Label</ReButton>
</template>

<script lang="ts">
  import { ReButton } from "@reservando/design-system";
</script>
```

## Using modifiers

<Canvas>
  <Story name="modifiers">
    {{
      components: { ReButton },
      data() {
        return {
          checked: true,
        };
      },
      template: `
      <div>
        <ReButton modifier="primary" class="mr-2">primary</ReButton>
        <ReButton modifier="secondary" class="mr-2">secondary</ReButton>
        <ReButton modifier="secondary-outline" class="mr-2">secondary-outline</ReButton>
        <ReButton modifier="link" class="mr-2">link</ReButton>
        <p class="hint mt-4">Disabled</p>
        <ReButton modifier="primary" disabled="true" class="mr-2">primary</ReButton>
        <ReButton modifier="secondary" disabled="true" class="mr-2">secondary</ReButton>
        <ReButton modifier="secondary-outline" disabled="true" class="mr-2">secondary-outline</ReButton>
        <ReButton modifier="link" disabled="true" class="mr-2">link</ReButton>
      </div>
    `,
    }}
  </Story>
</Canvas>

## Using sizes

<Canvas>
  <Story name="sizes">
    {{
      components: { ReButton },
      data() {
        return {
          checked: true,
        };
      },
      template: `
      <div>
        <ReButton size="small" class="mr-2">small</ReButton>
        <ReButton size="medium" class="mr-2">medium</ReButton>
        <ReButton size="large" class="mr-2">large</ReButton>
      </div>
    `,
    }}
  </Story>
</Canvas>
